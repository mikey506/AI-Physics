# Core dependencies
numpy
pygame
torch

# Optional dependencies
yfinance        # External data grounding
opencv-python   # Vision modulation
transformers    # Multimodal embeddings (DistilBERT)
qutip           # Quantum subgrid
